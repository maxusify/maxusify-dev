---
export interface Props {
  name: string;
  desc?: string;
}

const { name, desc } = Astro.props;
---

<div class="flex flex-col w-full justify-start pt-3">
  <h1 class="font-bold text-2xl text-rose-400">{name}</h1>
  {desc && <h3 class="font-thin text-xl text-slate-400">{desc}</h3>}
</div>
